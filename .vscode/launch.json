{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug a.out",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/a.out",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "Build (GNU++20 O2)",
      "stdio": [null, "${fileDirname}/input.txt", null]
    }
  ]
}